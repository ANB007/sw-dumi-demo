import { Layout as AntLayout } from 'antd';
import Layout from './layout';
import Content from './content';

const { Footer, Header, Sider } = AntLayout;

export { Layout, Content, Footer, Header, Sider };

export default Layout;
